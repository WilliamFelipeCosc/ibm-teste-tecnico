import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box component={"nav"} height={64} display='flex' alignItems={'center'}>
      Consumidor da API Google Books
    </Box>
  );
};

export default Header;
